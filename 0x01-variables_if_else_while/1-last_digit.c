#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main-Give output as last digits of random numbers inputed
 * Return: 0 (Success)
*/
int main(void)
{	int n;
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if
	last digit of n > 5;
	printf("%d n and is greater than 5 /n");
	else if
	last digit of n == 0;
	printf("%d n and is 0 /n");
	else if
	last digit of n < 6 ≠ 0;
	printf("%d n and is less than and not 0 /n");
	return (0);

}
